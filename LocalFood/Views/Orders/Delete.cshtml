@model LocalFood.Models.Order
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Видалити замовлення";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-lg border-0 rounded-4 my-5">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <div class="position-relative d-inline-block">
                            <div style="width: 100px; height: 100px; background: linear-gradient(45deg, var(--danger-color) 0%, #e02e56 100%); border-radius: 50%; filter: blur(20px); position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);"></div>
                            <i class="fa-solid fa-trash-can text-danger" style="font-size: 4rem; position: relative;"></i>
                        </div>
                    </div>

                    <h2 class="mb-4 fw-bold">Видалити замовлення #@Model.OrderId</h2>

                    <div class="card mb-4 border-0 bg-light rounded-4">
                        <div class="card-body p-4">
                            <ul class="list-unstyled text-start mb-0">
                                <li class="mb-2">
                                    <i class="fa-solid fa-user me-2 text-primary"></i>
                                    <strong>Клієнт:</strong> @Model.CustomerName
                                </li>
                                <li class="mb-2">
                                    <i class="fa-solid fa-phone me-2 text-primary"></i>
                                    <strong>Телефон:</strong> @Model.Phone
                                </li>
                                <li class="mb-2">
                                    <i class="fa-solid fa-location-dot me-2 text-primary"></i>
                                    <strong>Адреса:</strong> @Model.Address
                                </li>
                                <li class="mb-2">
                                    <i class="fa-solid fa-calendar-days me-2 text-primary"></i>
                                    <strong>Дата:</strong> @Model.OrderDate.ToString("dd.MM.yyyy HH:mm")
                                </li>
                                <li>
                                    <i class="fa-solid fa-money-bill-wave me-2 text-primary"></i>
                                    <strong>Сума:</strong> @Model.TotalAmount.ToString("0.00") грн
                                </li>
                            </ul>
                        </div>
                    </div>

                    <div class="alert alert-danger mb-4">
                        <i class="fa-solid fa-triangle-exclamation me-2"></i>
                        Ви дійсно хочете видалити це замовлення? Ця дія не може бути скасована.
                    </div>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="OrderId" />
                        <div class="d-flex justify-content-center gap-3">
                            <button type="submit" class="btn btn-danger btn-lg px-4">
                                <i class="fa-solid fa-check me-1"></i> Так, видалити
                            </button>
                            <a asp-action="Index" class="btn btn-secondary btn-lg px-4">
                                <i class="fa-solid fa-arrow-left me-1"></i> Ні, повернутись
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>