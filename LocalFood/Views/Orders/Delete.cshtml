@model LocalFood.Models.Order
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Видалити замовлення";
}

<h2 class="mb-4 text-danger">
    <i class="fa-solid fa-trash-can me-2"></i> Видалити замовлення
</h2>

<div class="card shadow-sm mb-4 border-danger bg-dark text-light">
    <div class="card-body">
        <p class="fs-5">
            <i class="fa-solid fa-circle-exclamation me-2 text-danger"></i>
            Ви дійсно хочете видалити замовлення <strong>#@Model.OrderId</strong>?
        </p>

        <ul class="list-unstyled ps-3 mb-4">
            <li><i class="fa-solid fa-user me-1 text-warning"></i> <strong>Клієнт:</strong> @Model.CustomerName</li>
            <li><i class="fa-solid fa-phone me-1 text-warning"></i> <strong>Телефон:</strong> @Model.Phone</li>
            <li><i class="fa-solid fa-location-dot me-1 text-warning"></i> <strong>Адреса:</strong> @Model.Address</li>
            <li><i class="fa-solid fa-calendar-days me-1 text-warning"></i> <strong>Дата:</strong> @Model.OrderDate.ToString("dd.MM.yyyy HH:mm")</li>
            <li><i class="fa-solid fa-sack-dollar me-1 text-warning"></i> <strong>Сума:</strong> @Model.TotalAmount.ToString("0.00") грн</li>
        </ul>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="OrderId" />
            <button type="submit" class="btn btn-danger me-2">
                <i class="fa-solid fa-trash me-1"></i> Так, видалити
            </button>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="fa-solid fa-arrow-left me-1"></i> Ні, повернутись
            </a>
        </form>
    </div>
</div>
